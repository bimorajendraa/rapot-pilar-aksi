DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306
DB_SSL=true
DB_CA_CERT=

# Auth (JWT)
JWT_SECRET=
JWT_EXPIRES_IN=12h

# Used only by `npm run seed:users` to create login accounts — never hardcode
# a default password in source code.
DEFAULT_EB_PASSWORD=
DEFAULT_DEPT_PASSWORD=
